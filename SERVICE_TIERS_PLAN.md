# 数学竞赛题批改：双档服务设计方案

> - 方案日期：2026-08-15
> - 文档状态：产品方案已确认，尚未实现
> - 代码基线：当前 `Garding-Miniprogram` 主分支
> - 本次范围：产品分档、价格快照、最小 UI 标识、评分链路、模型路由与 PDF 输出方案
> - 本次不做：不修改业务代码、不迁移数据库、不改变现有已确认 UI

## 0. 已确认的产品决策

| 项目 | 已确认决定 |
|---|---|
| 档位名称 | **简明评分 / 逐页精批** |
| 简明评分价格 | **¥1 / 答卷页**，暂不设置最低消费 |
| 逐页精批价格 | **¥5 / 答卷页** |
| 简明评分模型 | **`gpt-5.6-luna + max`** |
| 逐页精批模型 | 使用 **`gpt-5.6-sol + high`** |
| 简明评分交付 | **A4 XeLaTeX PDF**；总分、每题得分、主要问题和建议；不嵌入原答卷 |
| 逐页精批交付 | 保持现有原卷逐页标注 PDF |
| UI 原则 | 保持现有 Soft Geometry 风格、三步流程、动效、返回逻辑和页面结构 |

“UI 保持不变”指不重新设计界面与交互框架，并不代表前端完全零改动。只允许增加完成双档购买所必需的内容：

- 在现有“赛制与说明”页增加与评分标准选择器同风格的“批改方式”双选；
- 在确认支付、任务详情和历史记录中显示所购档位；
- 下载按钮根据档位显示“打开评分报告”或“打开批改报告”；
- 不增加页面、不改变导航和返回逻辑、不引入新的动画语言。

## 1. 结论

增加两个服务档位符合当前产品定位，而且能形成清晰的价格梯度：

1. **简明评分**面向“先知道得分和主要问题”的用户；
2. **逐页精批**面向“需要定位到原稿、理解每个得失分依据并据此订正”的用户。

正确的产品区分应当是**反馈粒度不同**，而不是“一个准确、一个不准确”。两个档位必须共享：

- 同一赛制 rubric；
- 同一题目理解与学生证明重建原则；
- 同一关键推理核验；
- 同一分数档位与算术校验；
- 同一怀疑式复核；
- 同一用户隔离、支付、下载和售后规则。

两者只在以下方面不同：

- 使用的受信运行 profile；
- 模型成本档位；
- 是否计算精确原稿标注位置；
- 是否生成原卷逐页标注版；
- 最终 PDF 的内容深度和页数。

不建议复制两套互相独立的 grader。最稳妥的结构是：

```text
共享评分核心
  ├── 简明评分渲染器：得分 + 主要问题 → A4 TeX 报告
  └── 逐页精批渲染器：原稿标注 + 右栏反馈 → 现有逐页 PDF
```

这样以后修改 IMO、CMO 或联赛 rubric 时，只维护一套评分规则，不会出现两个档位判分标准逐渐漂移。

---

## 2. 建议的产品定义

### 2.1 用户可见名称

建议不要使用“基础版 / 高级版”或“简单版 / 专业版”，避免让用户把简明档理解成随意评分。

推荐使用：

| 展示名称 | 内部 ID | 一句话说明 |
|---|---|---|
| 简明评分 | `summary_report` | 每题得分、主要问题与总体建议 |
| 逐页精批 | `annotated_review` | 原卷逐页标注、得失分依据与修改建议 |

### 2.2 功能与价格

| 项目 | 简明评分 | 逐页精批 |
|---|---|---|
| 目标用户 | 快速判断水平、考后估分、先找主要问题 | 深度复盘、竞赛备考、系统订正 |
| 每题得分 | 有 | 有 |
| 总分与赛制 | 有 | 有 |
| 每题主要问题 | 有，简洁列出 | 有，结合原稿定位详细解释 |
| 改进方向 | 每题一条简要建议 | 与原稿编号对应的具体建议 |
| 原卷框选/编号 | 无 | 有 |
| 逐页右栏反馈 | 无 | 有 |
| 原答卷嵌入结果 PDF | 无 | 有 |
| 最终输出 | A4 评分报告 PDF | 现有逐页标注 PDF |
| 建议模型 | `gpt-5.6-luna` | `gpt-5.6-sol` |
| 建议 reasoning effort | `max` | `high` |
| 确定价格 | **¥1 / 答卷页** | **¥5 / 答卷页** |
| 最低消费 | 暂无 | 暂无 |

两个价格都必须由版本化的服务端价格规则产生，并冻结到 quote 和订单中。小程序可以展示每页单价，但不得自行计算或决定应付金额。后续调整价格时，已经支付的订单继续使用原报价快照。

### 2.3 关于 “Luna + max”

官方 OpenAI 模型目录当前列出的模型 ID 是 `gpt-5.6-luna`，定位为成本敏感、高吞吐工作负载，并支持 `none / low / medium / high / xhigh / max` 推理档位：

- [OpenAI Models](https://developers.openai.com/api/docs/models)
- [OpenAI Model Comparison](https://developers.openai.com/api/docs/models/compare)

因此本方案可以使用：

```text
model = gpt-5.6-luna
model_reasoning_effort = max
```

但需要澄清：这里降低的是**模型成本档位**（Sol → Luna），不是 reasoning effort；`max` 仍是最高推理力度。若真正希望降低思考力度，应使用 `high` 或 `medium`，而不是 `max`。

本方案按用户当前明确意图采用 **Luna + max**。发布前必须以真实答卷评测决定是否保留；如果 Luna 无法达到评分质量门槛，即使成本更低也不能上线。

---

## 3. 当前代码为什么不能只加一个前端按钮

当前实现只有一套全局价格和一套输出契约：

| 当前限制 | 代码位置 | 对双档的影响 |
|---|---|---|
| `PriceRule` 只有 `cents_per_page`，没有档位 | [`server/models/orders.py`](server/models/orders.py) | 无法同时维护 ¥5/页和另一档价格 |
| Quote 只冻结 `grading_standard` | [`server/services/quotes.py`](server/services/quotes.py) | 付款后不知道用户购买了哪种报告 |
| GradingRound 不保存服务档位 | [`server/models/orders.py`](server/models/orders.py) | 复核轮次可能错用渲染方式 |
| TaskBundle 没有 `service_tier` | [`worker/runtime/contracts.py`](worker/runtime/contracts.py) | Worker 无法受信地选择模型和输出 |
| 模型曾写死为 Sol + high | [`worker/runtime/legacy/codex_runner.py`](worker/runtime/legacy/codex_runner.py) | 必须按档位路由 Luna |
| Skill 固定生成逐页标注 PDF | [`.agents/skills/olympiad-grader/SKILL.md`](.agents/skills/olympiad-grader/SKILL.md) | 简明档仍会承担全部标注与排版成本 |
| 输出页数强制为输入页数 + 1 | [`worker/runtime/legacy/codex_runner.py`](worker/runtime/legacy/codex_runner.py) | A4 简明报告会被错误拒绝 |
| Worker CLI 当前固定走 FakeGrader | [`worker/cli.py`](worker/cli.py) | 真实双档都尚未接入生产运行器 |

因此不能只修改 Prompt 和 PDF builder。若没有受信档位标识，Worker 无法知道该使用哪种模型和交付契约，支付后也无法证明用户购买了哪一档。正确的最小贯通范围是：

```text
小程序选择档位
→ 服务端报价与价格快照
→ 支付与订单
→ 批改轮次
→ Worker lease / TaskBundle
→ 受信 grading profile
→ 模型与 prompt 路由
→ JSON 契约
→ TeX 渲染器
→ Server 二次校验
→ 小程序查看和下载
```

---

## 4. 目标数据契约

### 4.1 受信档位字段

新增唯一字段：

```json
{
  "service_tier": "summary_report | annotated_review"
}
```

它必须由服务端校验并沿以下链路冻结：

```text
POST Quote
→ QuoteSession
→ Payment / Order
→ GradingRound
→ GradingJob lease
→ Worker LeasedTask
→ TaskBundle
→ config/grading-profile.json
→ grading.json / manifest.json
```

用户上传的 PDF、补充说明、参考 PDF 和网页内容都不得改变该值。

### 4.2 数据库建议

#### `price_rules`

增加：

```text
service_tier
currency = CNY
minimum_amount_cents
```

活动价格规则的唯一性应按档位处理，而不是全站只有一条价格。

#### `quote_sessions`

增加不可变快照：

```text
service_tier
unit_price_cents
minimum_amount_cents
quoted_amount_cents
pricing_version
```

现有 `price_rule_id` 继续保留。金额由服务端计算：

```text
max(page_count × unit_price_cents, minimum_amount_cents)
```

小程序只能展示返回值，不能自行计算。

首版已确认不设置最低消费，因此价格配置固定为：

```text
minimum_amount_cents = 0
summary_report.unit_price_cents = 100
annotated_review.unit_price_cents = 500
quoted_amount_cents = page_count × unit_price_cents
```

仍保留 `minimum_amount_cents` 字段，便于未来版本调整而不再迁移数据结构。

#### `grading_rounds`

增加：

```text
service_tier
runtime_profile_version
report_contract_version
```

V2 复核必须继承原订单档位，不能让复核从简明档升级为逐页精批，或反向降级。

### 4.3 Worker 任务快照

`TaskBundle` 建议增加：

```json
{
  "service_tier": "summary_report",
  "grading_standard": "imo",
  "league_scope": null,
  "runtime_profile_version": 1,
  "report_contract_version": 1
}
```

模型名称不由小程序直接提交。Worker 只按受信 server profile 映射：

```python
RUNTIME_PROFILES = {
    "summary_report": {
        "model": "gpt-5.6-luna",
        "reasoning_effort": "max",
        "report_mode": "summary",
    },
    "annotated_review": {
        "model": "gpt-5.6-sol",
        "reasoning_effort": "high",
        "report_mode": "annotated",
    },
}
```

该映射属于生产配置，不是用户输入。生产环境遇到未知档位、未知 profile 或 FakeGrader 必须 fail closed。

---

## 5. 共享评分链路

### 5.1 不应该省略的部分

简明档虽然不画框，仍必须完成：

1. 读取并检查所有答卷页；
2. 识别题目、条件和学生路线；
3. 按赛制建立题目专属评分节点；
4. 忠实拆解学生证明；
5. 核验关键推理、定理条件、计算、遗漏情形和结论；
6. 识别根本错误，避免重复扣分；
7. 映射到 IMO、CMO 或联赛合法分档；
8. 做一次怀疑式复核；
9. 校验总分、满分和分题算术。

这部分是产品可信度，不能因为价格低就删除。

### 5.2 简明档可以减少的部分

简明档可以省略：

- finding 的精确 `bbox / bboxes`；
- 原稿上的编号圆点和红/琥珀框；
- 每个输入页对应一张 A3 横向批改页；
- 右侧逐条反馈卡片；
- 原卷与标注图层的合成；
- 对标注错位、编号对应关系的视觉 QA。

proof map 仍需记录页码和大致位置，保证判分可审计，但不要求为每个公开意见计算紧框坐标。

### 5.3 推荐阶段

两个档位继续使用同一阶段 ID，前端无需维护两套状态机：

```text
preparing → understanding → rubric → decomposing → verifying
→ scoring → auditing → reporting → validating
```

差异只发生在 `reporting` 和 `validating`：

| 阶段 | 简明评分 | 逐页精批 |
|---|---|---|
| reporting | 生成摘要 JSON 和 A4 TeX 报告 | 生成 findings、标注层和逐页报告 |
| validating | 检查分题覆盖、算术、字体、裁切和空白页 | 额外检查框选、编号、原稿清晰度和逐页对应 |

---

## 6. 简明评分的公开结果契约

建议把公共结果拆成“共享评分部分 + 档位专属部分”。简明档示例：

```json
{
  "service_tier": "summary_report",
  "grading_standard": "imo",
  "resolved_league_scope": null,
  "title": "数学竞赛题评分报告",
  "total_score": 11,
  "max_score": 14,
  "overall_summary": "两题核心路线均成立，第二题结尾存在必要条件缺口。",
  "problems": [
    {
      "label": "第 1 题",
      "score": 7,
      "max_score": 7,
      "summary": "证明完整，关键条件均已说明。",
      "issues": [],
      "suggestion": "保持当前论证结构即可。"
    },
    {
      "label": "第 2 题",
      "score": 4,
      "max_score": 7,
      "summary": "主体构造有效，但结尾推论缺少必要论证。",
      "issues": [
        "未证明所构造对象满足题设中的唯一性条件。",
        "最后一步使用结论时遗漏了边界情形。"
      ],
      "suggestion": "补证唯一性，并单独讨论边界取等情况。"
    }
  ]
}
```

约束：

- 每题问题建议保持 0–3 条；没有问题时允许空数组；
- 不为了显得“内容多”而制造问题；
- 只公开决定分数的根本问题，不重复下游影响；
- 每题必须出现一次且分数与内部 `score-audit.json` 完全一致；
- 补充说明不能改变档位、赛制、模型、路径或 JSON 契约。

逐页精批继续使用现有 `pages[].findings[]` 契约，但根字段同样增加 `service_tier`。

---

## 7. 简明 TeX 报告设计

### 7.1 页面规格与最终模板

建议新增独立构建器：

```text
.agents/skills/olympiad-grader/scripts/build_summary_report.py
```

输出：

```text
output/report.pdf
```

最终确定使用 **A4 竖向**，而不是现有 A3 横向。纸张规格为 `210mm × 297mm`，建议页边距为上 `18mm`、下 `17mm`、左右 `18mm`。不嵌入原答卷图片，不生成 bbox、原稿编号或逐页标注页。

报告信息结构固定为：

1. 首页：报告标题、赛制、总分、总体结论；
2. 分题汇总：题号、得分、满分；
3. 每题区块：得分、判断、主要问题、改进建议；
4. 页脚：数学竞赛题批改、赛制和报告页码。

正常报告约 1–4 页，随题目数量和问题数量自然增长。不要为了填满页面增加无意义内容，也不要压缩到难以阅读。

#### 第一页模板

```text
┌──────────────────────────────────────────────┐
│ 数学竞赛题批改                      简明评分 │
│                                              │
│ 评分报告                                     │
│ 文件：答卷名称                               │
│ 赛制：IMO / CMO / 联赛二试        日期：……   │
│──────────────────────────────────────────────│
│                                              │
│   总分                                       │
│   18 / 21          总体判断：主要思路正确     │
│                                              │
│──────────────────────────────────────────────│
│ 分题概览                                     │
│  第 1 题     6 / 7      关键条件论证不足      │
│  第 2 题     7 / 7      论证完整              │
│  第 3 题     5 / 7      末段存在逻辑跳步      │
│──────────────────────────────────────────────│
│ 总体建议                                     │
│ 用 2–4 句总结最值得优先改进的证明习惯。       │
│                                              │
│ 数学竞赛题批改 · 对应赛制 · 第 1 / N 页      │
└──────────────────────────────────────────────┘
```

#### 分题页模板

每道题使用一个连续内容区块；内容短时允许两题同页，内容长时自然跨页，但标题与首段不得孤立在页尾。

```text
┌──────────────────────────────────────────────┐
│ 第 1 题                              6 / 7   │
│ 判断：方法正确，但一个关键条件说明不足        │
│──────────────────────────────────────────────│
│ 主要问题                                     │
│ 1. 使用某结论前没有验证其适用条件。           │
│ 2. 等号成立情形只断言，缺少必要说明。         │
│                                              │
│ 建议                                         │
│ 补写条件验证，并明确等号成立的充要条件。       │
└──────────────────────────────────────────────┘
```

公开报告只写决定得分的根本问题。正确且无需说明的题目可以写“论证完整，未发现影响得分的问题”，不能为了显得详细而制造意见。

### 7.2 视觉语言

继续沿用当前产品 UI：

- 暖白：`#F6F4EF` / `#FFFDFC`；
- 深墨：`#202320`；
- 学术绿：`#285A4A`；
- 浅绿：`#E8F0EB`；
- 朱红只用于实质错误；
- 同一 Noto CJK 字体和传统 LaTeX/AMS 数学公式；
- 使用 XeLaTeX，不使用 Unicode 数学符号拼排。

报告不需要机械复制小程序卡片，但颜色、层级与品牌气质必须一致。最终视觉规范冻结为：

- 页面背景保持白色或极浅暖白，保证打印与文件体积；
- 标题使用深墨 `#202320`，关键数字和分数使用学术绿 `#285A4A`；
- 总分摘要使用浅绿 `#E8F0EB` 底色和细绿色边线；
- 分题区块以留白和细分隔线组织，不堆叠厚重阴影；
- 实质错误可用克制的朱红标题或左侧细线，正文仍用深墨；
- 中文正文建议 10.5–11pt，分题标题 14–16pt，报告主标题 22–26pt；
- 行距约 `1.35–1.5`，数学公式交给 AMS/XeLaTeX 正常排版；
- 页眉不放技术模型名称，PDF 元数据和正文均不出现 Codex、Luna、Sol 等内部信息。

推荐输出文件名：

```text
<原文件名>_简明评分_<赛制>.pdf
```

### 7.3 PDF 校验分支

当前 runner 强制：

```text
逐页精批页数 = 原答卷页数 + 1
```

上线后必须按档位分支：

| 档位 | 页数规则 |
|---|---|
| `annotated_review` | 继续要求 `input_page_count + 1` |
| `summary_report` | `page_count >= 1`，且所有识别到的题目均出现在报告中 |

简明报告还必须检查：

- PDF 可打开；
- XeLaTeX 编译成功；
- 无空白页、裁切、乱码和公式错误；
- `service_tier`、赛制、分题得分、总分和满分与 JSON 一致；
- 输出中不包含学生原稿图片或误生成的逐页标注；
- 文件名使用 `<原名>_评分报告_<赛制>.pdf`。

逐页精批文件名继续使用 `<原名>_逐页批改_<赛制>.pdf`。

---

## 8. 小程序 UI 设计

### 8.1 保持现有 Soft Geometry 基线

继续遵守 [`Update.md`](Update.md) 中已经冻结的 UI：暖白、深墨、学术绿、圆角分组表面、系统字体、`160ms` 纯缩放反馈和现有导航逻辑。

不增加新的页面，不改底部 `首页 / 提交 / 任务`，不引入复杂动效。

### 8.2 创建流程

三步流程保持：

```text
选择文件 → 批改方式与赛制 → 确认支付
```

第二步在“选择评分标准”之前增加一个紧凑的双选组件：

```text
选择批改方式

┌ 简明评分                         ¥1 / 页 ┐
│ 每题得分、主要问题和 PDF 报告             │
└──────────────────────────────────────────┘

┌ 逐页精批                         ¥5 / 页 ┐
│ 原卷逐页标注、得失分依据和修改建议         │
└──────────────────────────────────────────┘
```

交互要求：

- 初始不默认选择，要求用户明确确认；
- 整行可点击，沿用现有评分标准选择器的边线、浅绿选中面和勾；
- 不使用营销式“最受欢迎”角标；
- 价格是服务端当前价格的预览，最终支付页仍以 quote 返回为准；
- 切换档位必须使已有 quote 失效并重新报价；
- 禁用、上传中和重复点击逻辑沿用现有实现。

### 8.3 支付页

确认列表增加：

```text
批改方式：简明评分 / 逐页精批
交付内容：评分报告 PDF / 逐页批改 PDF
```

费用明细继续只展示服务端返回：页数、每页服务费、总价和报价有效期。

### 8.4 任务与结果页

任务行和详情页增加克制的档位文案：

- `简明评分`；
- `逐页精批`。

不需要增加新颜色体系。两个档位都使用学术绿，靠文字区分，避免让简明档显得低质量。

结果按钮动态显示：

- 简明评分：`打开评分报告`；
- 逐页精批：`打开批改报告`。

分数区继续显示真实赛制满分：IMO 7 分档、CMO 21 分档、联赛 40/50 分档；不能因增加服务档位又退回统一 7 分制。

---

## 9. 复核、退款与用户承诺

建议两个档位都保留一次复核。原因是用户购买的是“可靠评分”，不是只购买排版。

区别是：

- 简明评分复核后重新生成简明报告；
- 逐页精批复核后重新生成逐页标注报告；
- 复核不能改变订单服务档位；
- 复核理由必须进入第二轮 grader 的受限输入；
- 退款资格、期限和金额仍由服务端计算。

对外文案不能写：

- “简明档准确率较低”；
- “低价模型评分”；
- “AI 自动判断，仅供娱乐”。

建议写：

- 简明评分：`保留完整判分，反馈聚焦每题得分与主要问题。`
- 逐页精批：`在完整判分基础上，增加原稿定位、逐页批注与修改建议。`

模型选择属于内部运行实现，用户购买的是服务结果和交付标准。

---

## 10. 质量门槛与评测

在收费上线前，建立同一批答卷的双档盲测集，至少覆盖：

- IMO：15 份；
- CMO：15 份；
- 联赛整卷与题组：各 10 份；
- 正确替代解法；
- 隐蔽关键错误；
- 未作答与部分作答；
- 模糊手写；
- 带参考 PDF 与不带参考 PDF。

同一答卷分别运行：

```text
Luna + max + summary_report
Sol + high + annotated_review
```

建议首发门槛：

| 指标 | 门槛 |
|---|---|
| 赛制、满分和分档正确 | 100% |
| 总分算术与分题之和一致 | 100% |
| 完全正确/致命错误方向判断 | 100% |
| 两档精确同分率 | ≥ 85% |
| 两档差异不超过一个赛制基础单位 | ≥ 95% |
| 简明报告每题覆盖率 | 100% |
| PDF 可打开、无乱码裁切 | 100% |
| Luna 单任务中位运行成本 | 明显低于逐页精批 |
| Luna 单任务中位耗时 | 不高于逐页精批的 70% |

基础单位：IMO 1 分、CMO 3 分、联赛 10 分。

如果 Luna 未达到质量门槛，优先顺序应是：

1. 优化简明 prompt 和证据校验；
2. 将 Luna effort 保持 `max`；
3. 必要时将简明档切换到 Terra；
4. 仍不达标则不能以降低评分质量换低价。

---

## 11. 实施顺序

### Phase A：契约和价格

1. 增加 `service_tier`、分档价格规则和数据库迁移；
2. Quote/Order/Round/Lease 全链路快照；
3. 旧演示订单迁移为 `annotated_review` 并标记 `is_demo`；
4. 修复测试配置被本地 `.env` 污染的问题；
5. 服务端测试验证两档报价、最低消费、重复支付和快照不变。

### Phase B：共享评分核心与双渲染器

1. 先完成 [`Update.md`](Update.md) 的真实 Worker P0：runtime factory、league scope、Skill 隔离和 Server 结果校验；
2. 在受信 profile 中增加 `service_tier/report_mode`；
3. 保留一套 staged grading contract；
4. 新增简明 JSON schema 与 A4 XeLaTeX builder；
5. 让 manifest/page-count 校验按档位分支；
6. 真实接入 Luna + max 和 Sol + high。

### Phase C：小程序 UI

1. 在第二步增加“批改方式”选择；
2. 报价请求增加 `service_tier`；
3. 支付页、任务行、详情页和下载按钮显示档位；
4. 保持现有 Soft Geometry、返回逻辑和局部轮询；
5. 320/375/390/430px 与真机回归。

### Phase D：试运营

1. 先只开放内部用户；
2. 对同一评测集双跑；
3. 统计每页成本、每单固定成本、P50/P95 延迟、复核率与退款率；
4. 确认简明档正式价格；
5. 小流量开放，再逐步扩容。

---

## 12. 必须新增的测试

### 服务端

- 两档价格规则可同时生效；
- Quote 切换档位后旧报价失效；
- 支付后档位不可修改；
- V2 复核继承 V1 档位；
- 不同档位结果不可串单；
- Worker 提交错误 `service_tier/report_type` 时 Server 拒绝；
- 退款金额继续使用订单实付快照。

### Worker

- summary 任务只能路由到 Luna profile；
- annotated 任务只能路由到 Sol profile；
- 用户说明不能切换模型或档位；
- summary 不生成 bbox、原稿标注或 A3 页面；
- annotated 继续严格生成输入页数 + 1；
- 两档均执行赛制、满分、分档和总分校验；
- 两档网络重试、取消、超时和进程树回收互不串扰。

### 小程序

- 未选择批改方式不能报价；
- 切换档位清除旧 quote；
- 价格只展示服务端返回值；
- 支付确认明确显示档位和交付物；
- 历史订单能区分两档；
- 下载按钮文案与实际 PDF 一致；
- UI Token、按压反馈、返回逻辑和轮询不回退。

### PDF

- 简明报告覆盖全部题目；
- 中文、公式、长问题说明不裁切；
- 无空白页或乱码；
- 总分与 JSON 完全一致；
- 逐页精批既有标注 QA 不退化。

---

## 13. 产品风险

| 风险 | 控制方式 |
|---|---|
| 简明档抢走逐页精批需求 | 明确“是否需要原稿定位与订正”的场景差异，不用模糊营销文案 |
| 用户误以为简明档判分随意 | 两档共享评分标准与复核，公开强调反馈粒度不同 |
| Luna 分数与 Sol 波动较大 | 双跑评测；不达门槛则换 Terra 或暂缓上线 |
| 简明档售后成本侵蚀毛利 | 首版按 ¥1/页运行，监控单均页数、复核率和退款率，再决定是否引入最低消费 |
| 两套 schema/renderer 漂移 | 共享评分核心，公共字段统一，档位只分叉在报告阶段 |
| 前端价格与后端不一致 | 所有金额由服务端 quote 返回，前端不计算 |
| 旧订单无法识别档位 | 数据迁移 + `is_demo` + 默认旧报告为 annotated，仅用于历史展示 |

---

## 14. 最终实施边界

产品决策已经确认，不再保留待定项。

### 保持不变

- 现有 Soft Geometry 视觉体系、字号、圆角、配色和主视觉；
- 首页、提交、任务三个主导航及三步提交流程；
- 已确认的按压动画、返回逻辑、局部轮询和安全区；
- 微信登录、用户隔离、支付、订单、退款和复核的业务含义；
- IMO、CMO、联赛二试的赛制选择与评分分档。

### 最小必要改动

1. **小程序**：增加批改方式双选和档位展示，不重做页面；
2. **Server/数据库**：增加受信 `service_tier`、档位价格规则和不可变报价/订单快照；
3. **Worker 契约**：把 `service_tier` 贯通到 TaskBundle 和受信 profile；
4. **评分运行器**：共享评分核心，按档位选择 Luna/max 或 Sol/high；
5. **交付渲染**：简明档走新的 A4 XeLaTeX builder，逐页精批保持现有 builder；
6. **结果校验**：Server 与 Worker 都核对档位、赛制、分数、报告类型和 PDF 完整性。

不应修改与该功能无关的 UI、导航、动画、账户页面或售后流程。也不得仅依靠文件名或 Prompt 判断档位；唯一权威标识必须是服务端冻结的：

```json
{
  "service_tier": "summary_report | annotated_review"
}
```

实施建议拆成数据库/Server、Worker 契约、评分与 PDF、小程序四个可独立审查的 PR。每个 PR 都先通过原有测试，再进入下一层，避免价格、订单和交付契约同时失控。

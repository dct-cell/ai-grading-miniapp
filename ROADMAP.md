# 真实上线待办

本文件只记录当前尚未完成、会阻止真实收费上线的事项。已经落地的历史阶段和一次性实施步骤不再保存在仓库中，具体实现以代码、迁移和测试为准。

## P0：上线前必须完成

### 微信与支付

- 接入真实微信登录，服务端校验微信返回身份，不接受客户端自报用户。
- 接入微信支付预下单、签名验证、回调验签和服务端查单。
- 接入真实退款，并确认支付方按 `external_refund_id` 幂等去重。
- production 完全禁用假登录、假支付和无签名回调。
- 补齐真实支付、重复回调、退款重试和金额快照的端到端测试。

### 部署与数据安全

- 增加 API、scheduler 和管理后台的可审计部署方式及启停脚本。
- 使用 MySQL 8，隔离 staging 与 production 的数据库、文件、密钥和支付配置。
- 配置 HTTPS、域名、反向代理、最小端口暴露和日志轮转。
- 建立加密备份、保留策略、恢复演练和备份新鲜度告警；未完成恢复演练前不得宣称有可用备份。
- 从配置模型和 `.env.example` 移除已经废弃的 `GRADER_ADMIN_SHARED_KEY`。
- 运维资料不得提交公网 IP、私钥路径、主机清单、密钥或生产数据库连接串。

### 真实链路验收

- 在真实 MySQL 上验证多 Worker 并发领取、租约 fencing、scheduler 单例和退款互斥。
- 使用真实 Codex 与 XeLaTeX 对联赛二试、CMO、IMO 建立固定回归样例，分别覆盖简明评分和逐页精批。
- 验证退款后下载立即返回 410、跨用户文件不可见、不同档位与赛制不会串单。
- 完成小程序真机上传、支付、下载、复核、退款和弱网/中断测试。
- 完成 Admin 登录、CSRF、权限边界、价格版本化和审计记录人工验收。

## P1：内测稳定后完成

- 提供 macOS、Linux、Windows Worker 安装、升级、回滚和卸载流程。
- 建立队列长度、批改时延、失败率、复核率、退款率和每页成本监控。
- 对超时、断网、Codex/XeLaTeX 进程树残留和磁盘不足增加告警与恢复验证。
- 根据真实样本调整 Worker 容量和档位开放策略；简明评分默认保持可关闭。
- 清理 README、miniapp 和 admin 文档中的测试数量，避免长期保存会过期的固定数字。

## 外部前置条件

- 可用域名、HTTPS 证书及所需备案/平台配置。
- 微信小程序正式 AppID、请求域名配置和发布权限。
- 微信商户号、支付与退款凭据，以及回调地址。
- production 数据库、文件存储、备份位置和告警接收渠道。

以上条件未满足并完成验收前，系统只用于本地开发或受控内测。
